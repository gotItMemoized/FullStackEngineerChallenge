import React from 'react';
import { SectionContent } from '../components';

export default () => {
  return (
    <section className="hero is-dark is-bold is-fullheight-with-navbar">
      <SectionContent>
        <h1 className="title">Could not find that page</h1>
      </SectionContent>
    </section>
  );
};
